generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Word {
  id             Int           @id @default(autoincrement())
  value          String        @unique
  categories     Json          @default("[]")

  @@map("words")
}